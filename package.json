{
  "name": "picklog",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint **/*.js",
    "changelog": "node ./bin/picklog.js > CHANGELOG.md && git add -A . && git commit -m \"changelog\"",
    "preversion": "npm run lint && npm test",
    "postversion": "npm run changelog && npm publish .  && git push && git push --tags"
  },
  "husky": {
    "hooks": {
      "pre-push": "npm run lint"
    }
  },
  "bin": {
    "picklog": "./bin/picklog.js"
  },
  "author": "BearJ",
  "license": "MIT",
  "engines": {
    "node": ">=6.0.0"
  },
  "dependencies": {
    "object.values": "^1.1.0",
    "spawn-error-forwarder": "^1.0.0",
    "through2": "^3.0.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.2",
    "eslint": "^5.12.1",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.16.0",
    "husky": "^1.3.1",
    "jest": "^24.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BearJ/picklog.git"
  },
  "keywords": [
    "changelog",
    "git log",
    "log",
    "generate log"
  ]
}
